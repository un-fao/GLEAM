# Cohort code -\> full name mapping

Maps each cohort short code to its descriptive name (sex and age class).
`gleam_cohorts` is derived from this as the vector of codes.

## Usage

``` r
gleam_cohorts_names
```

## Format

A named character vector of length 6.
