# Species code -\> full name mapping

Maps each species short code to its full common name. Used for display
and documentation. `gleam_species` is derived from this as the vector of
codes.

## Usage

``` r
gleam_species_names
```

## Format

A named character vector of length 6.
