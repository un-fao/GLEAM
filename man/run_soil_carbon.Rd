% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_soil_carbon_module.R
\name{run_soil_carbon}
\alias{run_soil_carbon}
\title{Run Soil Carbon Stock Change Calculation (Internal)}
\usage{
run_soil_carbon(data, management_params, soil_type_params, luc_factors)
}
\arguments{
\item{data}{Data.table. Input dataset containing columns:
\code{area}, \code{climate_zone}, \code{soil_carbon_reference} (optional), \code{soil_type},
\code{management_start}, \code{management_end}.}

\item{management_params}{Data.table. Management factors by climate zone
and management type. Must have columns: \code{climate_zone}, \code{management_type}, \code{V1}.}

\item{soil_type_params}{Data.table. SOC reference values by climate zone
and soil type. Must have columns: \code{climate_zone}, \code{soil_type}, \code{V1}.}

\item{luc_factors}{Data.table. Land-use change factors by climate zone.
Must have columns: \code{climate_zone}, \code{V1}.}
}
\value{
Data.table. The input data with three new columns:
\describe{
\item{SOC1}{SOC stock at start (t C total).}
\item{SOC2}{SOC stock at end (t C total).}
\item{dSOC}{Annual SOC change (t C/year total).}
}
}
\description{
Executes the soil organic carbon (SOC) stock change pipeline.
Applies the core soil carbon model row-by-row to an input dataset using
parameter tables for management factors, soil type references, and land-use change factors.
}
\examples{
\dontrun{
input <- data.table::fread(
  system.file("extdata/soc_input_data.csv", package = "gleam")
)
data_out <- run_soil_carbon(
  data = input,
  management_params = data.table::fread(system.file(
    "extdata/SoilCarbon_parameters/soc_factor_management.csv",
    package = "gleam")),
  soil_type_params = data.table::fread(system.file(
    "extdata/SoilCarbon_parameters/soc_socref_soiltype.csv",
    package = "gleam")),
  luc_factors = data.table::fread(system.file(
    "extdata/SoilCarbon_parameters/soc_luc_factor.csv",
    package = "gleam"))
)
}

}
\keyword{internal}
