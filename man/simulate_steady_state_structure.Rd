% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/herd_simulation_core_model.R
\name{simulate_steady_state_structure}
\alias{simulate_steady_state_structure}
\title{Simulate Steady-State Population Structure}
\usage{
simulate_steady_state_structure(
  initial_structure,
  max_years,
  min_lambda_change,
  fem_fec,
  mal_fec,
  prob_death,
  prob_offtake,
  prob_growth,
  prop_non_demo
)
}
\arguments{
\item{initial_structure}{Named numeric vector of length 6. Initial number of individuals in each of the 6
sex-age classes. Must be named with: \code{FJ}, \code{FS}, \code{FA}, \code{MJ}, \code{MS}, \code{MA}.}

\item{max_years}{Integer. Maximum number of years to simulate.}

\item{min_lambda_change}{Numeric. Threshold for minimal change in class-specific growth rates to reach
steady state.}

\item{fem_fec}{Numeric. Daily number of female births per adult female.}

\item{mal_fec}{Numeric. Daily number of male births per adult female.}

\item{prob_death}{Named numeric vector of length 10. Daily death probabilities for 10 cohorts. Must be named
using: \code{FB}, \code{FJ}, \code{FS}, \code{FA}, \code{FC}, \code{MB}, \code{MJ}, \code{MS},
\code{MA}, \code{MC}.}

\item{prob_offtake}{Named numeric vector of length 10. Daily offtake probabilities for 10 cohorts.
Names must match those in \code{prob_death}.}

\item{prob_growth}{Named numeric vector of length 10. Daily probability of transition
to the next class for 10 cohorts. Names must match those in \code{prob_death}.}

\item{prop_non_demo}{Named numeric vector. Fraction (0--1) of individuals \emph{diverted into the
non-demographic stream at the moment they transition to the next age class}.
Must be named with the transitioning demographic cohorts:
\code{FJ}, \code{FS}, \code{FA}, \code{MJ}, \code{MS}, \code{MA}.
For example, \code{prop_non_demo["FJ"]} applies to individuals growing from \code{FJ -> FS};
\code{prop_non_demo["FA"]} applies to individuals growing from \code{FA -> FC}.}
}
\value{
A named list with:
\describe{
\item{days_steady}{Number of days simulated until steady state is reached (or until the loop ends).}
\item{structure}{Final share of each of the 8 cohorts: \code{FB}, \code{FJ}, \code{FS}, \code{FA}, \code{MB}, \code{MJ}, \code{MS}, \code{MA}.}
\item{share}{Final share of the 6 grouped classes (\code{FJ} includes \code{FB+FJ}; \code{MJ} includes \code{MB+MJ}).}
\item{growth_rate_pop}{Annualized population growth rate at steady state.}
\item{size_unscaled}{Final unscaled population sizes for the 6 grouped classes.}
\item{size_total_demo}{Total demographic population size at the end of the simulation (sum of grouped classes).}
}
}
\description{
Simulates population dynamics over time until a steady-state is reached. Tracks age-class structure and
population growth based on survival, offtake, and fecundity parameters.
}
