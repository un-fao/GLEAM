% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soil_carbon_core_model.R
\name{calc_soil_carbon}
\alias{calc_soil_carbon}
\title{Calculate Soil Organic Carbon Stock Change}
\usage{
calc_soil_carbon(
  area,
  climate_zone,
  soil_carbon_reference = NA_real_,
  soil_type,
  management_start,
  management_end,
  management_params,
  soil_type_params,
  luc_factors
)
}
\arguments{
\item{area}{Numeric. Land area (ha).}

\item{climate_zone}{Character. IPCC climate zone code (e.g., "TropicalMoist", "WarmTemperateDry").}

\item{soil_carbon_reference}{Numeric. Reference SOC value (t C/ha, optional). If missing, will be looked up from \code{soil_type_params}.}

\item{soil_type}{Character. IPCC soil type code (e.g., "HighActivityClay", "LowActivityClay", "Sandy", "Spodic"). Required if \code{soil_carbon_reference} is missing.}

\item{management_start}{Character. Starting management type (e.g., "ImprovedMediumInput", "SeverelyDegraded").}

\item{management_end}{Character. Ending management type (e.g., "NonDegraded", "HighIntensityGrazing").}

\item{management_params}{Data.table. Stock change factors for management regime (dimensionless). Must have columns: \code{climate_zone}, \code{management_type}, \code{V1}.}

\item{soil_type_params}{Data.table. Reference SOC values by climate zone and soil type (t C/ha). Must have columns: \code{climate_zone}, \code{soil_type}, \code{V1}.}

\item{luc_factors}{Data.table. Stock change factors for land use or land-use change type (dimensionless). Must have columns: \code{climate_zone}, \code{V1}.}
}
\value{
List. Contains:
\describe{
\item{SOC1}{Soil organic carbon stock at the beginning of the period (t C total).}
\item{SOC2}{Soil organic carbon stock at the end of the period (t C total).}
\item{dSOC}{Annual change in soil carbon stock over 20 years (t C/year total).}
}
}
\description{
Computes the change in soil organic carbon (SOC) stock between two management states over a 20-year period.
This is a Tier 1 stock-change equation with a fixed 20-year transition period.
}
\details{
The core calculation is:
\deqn{SOC1 = area \times socRef \times mgmtFactor_{start} \times lucFactor}
\deqn{SOC2 = area \times socRef \times mgmtFactor_{end} \times lucFactor}
\deqn{dSOC = (SOC2 - SOC1) / 20}
}
