% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_all_herd_module.R
\name{run_all_herd_module}
\alias{run_all_herd_module}
\title{Run Combined Herd Module Pipeline}
\usage{
run_all_herd_module(
  cohort_level_data = NULL,
  herd_level_data = NULL,
  simulation_duration = 365,
  run_demographic = TRUE,
  run_nondemographic = TRUE
)
}
\arguments{
\item{cohort_level_data}{A \code{data.table} with one row per \code{herd_id} and cohort.
Required rows and columns depend on the selected execution mode:
\itemize{
\item \strong{Case 1 - when \code{run_demographic = TRUE} and \code{run_nondemographic = FALSE}:}
\code{cohort_level_data} must contain rows for the demographic cohorts only
(\code{FA}, \code{FJ}, \code{FS}, \code{MA}, \code{MJ}, \code{MS}). In this case the table must contain
the columns required by \code{\link[=run_demographic_herd_module]{run_demographic_herd_module()}}:
\describe{
\item{\code{herd_id}}{Character or numeric. Unique identifier for the herd,
repeated for each cohort belonging to the same herd.}
\item{\code{cohort_short}}{Character. Demographic cohort code. Supported
values are \code{FJ}, \code{FS}, \code{FA}, \code{MJ}, \code{MS}, and \code{MA}.}
\item{\code{cohort_duration_days}}{Numeric. Amount of time that each animal
spends in the demographic cohort (days).}
\item{\code{offtake_rate}}{Numeric. Annual proportion of animals removed
from the herd for each demographic cohort (fraction).}
\item{\code{death_rate}}{Numeric. Fraction of deaths in a herd over a year
for each demographic cohort (fraction).}
}
\item \strong{Case 2 - when \code{run_demographic = FALSE} and \code{run_nondemographic = TRUE}:}
\code{cohort_level_data} must contain rows for the non-demographic cohorts
only (\code{FN}, \code{MN}). In this case the table must contain the columns
required by \code{\link[=run_nondemographic_herd_module]{run_nondemographic_herd_module()}}:
\describe{
\item{\code{herd_id}}{Character or numeric. Unique identifier for the herd,
repeated for each non-demographic cohort phase belonging to the same herd.}
\item{\code{cohort_short}}{Character. Non-demographic cohort code.
Supported values are \code{FN} for non-demographic female animals and \code{MN}
for non-demographic male animals.}
\item{\code{nondemo_productive_phase_id}}{Numeric. Productive phase
identifier within a non-demographic cycle. \code{1} is required and \code{2} is
optional.}
\item{\code{death_rate}}{Numeric. Fraction of deaths occurring during the
non-demographic productive phase (fraction).}
}
\item \strong{Case 3 - when \code{run_demographic = TRUE} and \code{run_nondemographic = TRUE}:}
\code{cohort_level_data} must contain both demographic and non-demographic
rows. In this case the table must satisfy both sets of requirements
described above. Internally, \code{run_all_herd_module()} splits the combined
input into demographic and non-demographic subsets using \code{cohort_short}.
}}

\item{herd_level_data}{A \code{data.table} with one row per \code{herd_id}. This table
is required in all execution modes. Required columns depend on the selected
execution mode:
\itemize{
\item \strong{Case 1 - when \code{run_demographic = TRUE} and \code{run_nondemographic = FALSE}:}
\code{herd_level_data} must contain the herd-level inputs required by
\code{\link[=run_demographic_herd_module]{run_demographic_herd_module()}}:
\describe{
\item{\code{herd_id}}{Character or numeric. Unique identifier for the herd.}
\item{\code{parturition_rate}}{Numeric. Average annual number of
parturitions per female animal (# parturitions/adult female/year).}
\item{\code{litter_size}}{Numeric. Average number of offspring born per
parturition (# offspring/parturition).}
\item{\code{birth_fraction_female}}{Numeric. Female birth fraction,
defined as the probability that a newborn offspring is female
(fraction).}
\item{\code{herd_size_total}}{Numeric. Total population size at the start of
the year, including all cohorts (# heads).}
\item{\code{prop_nondemo_mal_juv}}{Numeric. Fraction of male juveniles
diverted into the non-demographic stream at the moment they transition
to the next age class (fraction).}
\item{\code{prop_nondemo_fem_juv}}{Numeric. Fraction of female juveniles
diverted into the non-demographic stream at the moment they transition
to the next age class (fraction).}
}
\item \strong{Case 2 - when \code{run_demographic = FALSE} and \code{run_nondemographic = TRUE}:}
\code{herd_level_data} must contain the herd-level inputs required by
\code{\link[=run_nondemographic_herd_module]{run_nondemographic_herd_module()}}:
\describe{
\item{\code{herd_id}}{Character or numeric. Unique identifier for the herd.}
\item{\code{cohort_stock_fem_annual_nondemo}}{Numeric. Total annual entrants
into the \code{FN} cohort block over the simulated period (# heads /
simulated period).}
\item{\code{cohort_stock_mal_annual_nondemo}}{Numeric. Total annual entrants
into the \code{MN} cohort block over the simulated period (# heads /
simulated period).}
\item{\code{rest_between_nondemo_cycles_duration}}{Numeric. Duration of the
resting or empty phase between non-demographic cycles (days).}
\item{\code{phase1_nondemo_fem_duration_days}}{Numeric. Duration of
productive phase 1 for the female non-demographic cohort (\code{FN}) (days).}
\item{\code{phase2_nondemo_fem_duration_days}}{Numeric. Duration of
productive phase 2 for the female non-demographic cohort (\code{FN}) (days).}
\item{\code{phase1_nondemo_mal_duration_days}}{Numeric. Duration of
productive phase 1 for the male non-demographic cohort (\code{MN}) (days).}
\item{\code{phase2_nondemo_mal_duration_days}}{Numeric. Duration of
productive phase 2 for the male non-demographic cohort (\code{MN}) (days).}
}
\item \strong{Case 3 - when \code{run_demographic = TRUE} and \code{run_nondemographic = TRUE}:}
\code{herd_level_data} must contain the demographic herd inputs required by
\code{\link[=run_demographic_herd_module]{run_demographic_herd_module()}}, plus the non-demographic cycle-duration
inputs required to run \code{\link[=run_nondemographic_herd_module]{run_nondemographic_herd_module()}} after annual
non-demographic entrants are derived internally from the demographic output:
\describe{
\item{\code{herd_id}}{Character or numeric. Unique identifier for the herd.}
\item{\code{parturition_rate}}{Numeric. Average annual number of
parturitions per female animal (# parturitions/adult female/year).}
\item{\code{litter_size}}{Numeric. Average number of offspring born per
parturition (# offspring/parturition).}
\item{\code{birth_fraction_female}}{Numeric. Female birth fraction,
defined as the probability that a newborn offspring is female
(fraction).}
\item{\code{herd_size_total}}{Numeric. Total population size at the start of
the year, including all cohorts (# heads).}
\item{\code{prop_nondemo_mal_juv}}{Numeric. Fraction of male juveniles
diverted into the non-demographic stream at the moment they transition
to the next age class (fraction).}
\item{\code{prop_nondemo_fem_juv}}{Numeric. Fraction of female juveniles
diverted into the non-demographic stream at the moment they transition
to the next age class (fraction).}
\item{\code{rest_between_nondemo_cycles_duration}}{Numeric. Duration of the
resting or empty phase between non-demographic cycles (days).}
\item{\code{phase1_nondemo_fem_duration_days}}{Numeric. Duration of
productive phase 1 for the female non-demographic cohort (\code{FN}) (days).}
\item{\code{phase2_nondemo_fem_duration_days}}{Numeric. Duration of
productive phase 2 for the female non-demographic cohort (\code{FN}) (days).}
\item{\code{phase1_nondemo_mal_duration_days}}{Numeric. Duration of
productive phase 1 for the male non-demographic cohort (\code{MN}) (days).}
\item{\code{phase2_nondemo_mal_duration_days}}{Numeric. Duration of
productive phase 2 for the male non-demographic cohort (\code{MN}) (days).}
}
In this combined mode, \code{cohort_stock_fem_annual_nondemo} and
\code{cohort_stock_mal_annual_nondemo} are omitted because derived internally from the
demographic output and therefore do not need to be supplied as primary
herd-level inputs.
}}

\item{simulation_duration}{Numeric. Length of the reporting period (days).}

\item{run_demographic}{Logical. If \code{TRUE}, run
\code{\link[=run_demographic_herd_module]{run_demographic_herd_module()}}.}

\item{run_nondemographic}{Logical. If \code{TRUE}, run
\code{\link[=run_nondemographic_herd_module]{run_nondemographic_herd_module()}}.}
}
\value{
A named list with two elements:
\describe{
\item{\code{cohort_level_results}}{A \code{data.table} with one row per retained
cohort. Depending on the selected execution mode, this table contains
demographic rows only, non-demographic rows only, or both in a harmonized
schema. It includes the original cohort-level input columns plus the
following result variables when available:
\describe{
\item{\code{cohort_stock_size_unscaled}}{Numeric. Average cohort stock before
final harmonization to \code{herd_size_total} (# heads).}
\item{\code{cohort_stock_size}}{Numeric. Final average cohort stock after
harmonization of the output schema. When both modules are run together,
this is the rescaled stock size; when only one module is run, this is the
renamed value of \code{cohort_stock_size_unscaled} (# heads).}
\item{\code{offtake_heads}}{Numeric. Total number of animals removed from the
cohort over the full 365-day simulation horizon (# heads).}
\item{\code{offtake_heads_assessment}}{Numeric. Total number of animals removed
from the cohort over \code{simulation_duration} (# heads / simulated period).}
\item{\code{cohort_stock_annual_nondemographic}}{Numeric. For demographic
rows only, annual number of animals diverted from the demographic pathway
into the non-demographic pathway (# heads / simulated period).}
\item{\code{nondemo_productive_phase_id}}{Numeric. For non-demographic rows
only, productive phase identifier within the non-demographic cycle.
Allowed values are \code{1} and optionally \code{2}.}
\item{\code{partial_nondemo_phase_duration}}{Numeric. For non-demographic rows
only, duration of the terminal partial productive phase falling within the
fixed 365-day non-demographic simulation horizon (days).}
\item{\code{number_full_nondemo_cycles}}{Integer. For non-demographic rows
only, number of complete non-demographic cycles fully contained within the
fixed 365-day simulation horizon (cycles / simulated period).}
\item{\code{total_nondemo_cycle_starts_to_distribute}}{Integer. For
non-demographic rows only, total number of cycle starts within the fixed
365-day simulation horizon used to distribute annual entrants (cycle
starts / simulated period).}
}}
\item{\code{herd_level_results}}{A \code{data.table} with one row per \code{herd_id}.
Depending on the selected execution mode, this is the herd-level output of
the demographic module, the non-demographic module, or the merged herd-level
output of both modules. It includes the original herd-level input columns
plus the following result variables when available:
\describe{
\item{\code{growth_rate_herd}}{Numeric. Annualized herd growth rate estimated
by the demographic herd module (fraction).}
\item{\code{cohort_stock_fem_annual_nondemo}}{Numeric. Annual number of female
animals entering the non-demographic pathway (\code{FN}) over the simulated
period (# heads / simulated period). When both modules are run, this is
derived internally from the demographic output.}
\item{\code{cohort_stock_mal_annual_nondemo}}{Numeric. Annual number of male
animals entering the non-demographic pathway (\code{MN}) over the simulated
period (# heads / simulated period). When both modules are run, this is
derived internally from the demographic output.}
\item{\code{rest_between_nondemo_cycles_duration}}{Numeric. Duration of the
resting or empty phase between non-demographic cycles (days).}
\item{\code{total_nondemo_fem_duration_days}}{Numeric. Total duration of the
productive non-demographic phases assigned to the female non-demographic
cohort block (\code{FN}) (days).}
\item{\code{total_nondemo_mal_duration_days}}{Numeric. Total duration of the
productive non-demographic phases assigned to the male non-demographic
cohort block (\code{MN}) (days).}
}}
}
}
\description{
This function orchestrates the demographic herd module
\code{\link[=run_demographic_herd_module]{run_demographic_herd_module()}} and the non-demographic herd module
\code{\link[=run_nondemographic_herd_module]{run_nondemographic_herd_module()}} within a single wrapper. Depending on the
selected logical switches, it runs the demographic module, the
non-demographic module, or both, and returns a harmonized herd-module output
structure for downstream use in \code{\link[=run_gleam]{run_gleam()}}.
}
\details{
The function supports three execution modes:
\subsection{1. Demographic only (see \code{\link[=run_demographic_herd_module]{run_demographic_herd_module()}} for additional details)}{
}

\subsection{2. Non-demographic only (see \code{\link[=run_nondemographic_herd_module]{run_nondemographic_herd_module()}} for additional details)}{
}

\subsection{3. Demographic and non-demographic together}{

When \code{run_demographic = TRUE} and \code{run_nondemographic = TRUE}, the function:
\enumerate{
\item runs \code{\link[=run_demographic_herd_module]{run_demographic_herd_module()}} on the demographic subset
\item derives annual non-demographic entrants from the demographic output
\item runs \code{\link[=run_nondemographic_herd_module]{run_nondemographic_herd_module()}} on the non-demographic subset
\item row-binds the two cohort-level result tables
\item harmonizes shared output columns
\item rescales average stock and offtake to the herd total
}

When both modules are run, annual entrants into the non-demographic blocks
are derived from the demographic output:
\itemize{
\item female entrants to \code{FN} are taken from demographic cohort \code{FJ} as
\code{cohort_stock_fem_annual_nondemo = cohort_stock_annual_nondemographic}
\item male entrants to \code{MN} are taken from demographic cohort \code{MJ} as
\code{cohort_stock_mal_annual_nondemo = cohort_stock_annual_nondemographic}
}

These entrant counts are merged with herd-level non-demographic timing inputs
such as:
\itemize{
\item \code{rest_between_nondemo_cycles_duration}
\item \code{phase1_nondemo_fem_duration_days}
\item \code{phase2_nondemo_fem_duration_days}
\item \code{phase1_nondemo_mal_duration_days}
\item \code{phase2_nondemo_mal_duration_days}
}

After the selected modules run, the function combines their cohort-level
outputs using \code{data.table::rbindlist(..., use.names = TRUE, fill = TRUE)}.
This allows demographic and non-demographic rows to share a common schema.

The combined cohort output harmonizes, when needed:
\itemize{
\item \code{offtake_heads}
\item \code{offtake_heads_assessment}
\item \code{cohort_duration_days}
\item \code{death_rate}
\item \code{cohort_stock_size_unscaled}
\item \code{cohort_stock_size}
}

The function then rescales the combined cohort
stock and offtake outputs so that the summed cohort average stock matches
\code{herd_size_total} from \code{herd_level_data}.

The rescaling is performed in two steps:
\enumerate{
\item \code{cohort_stock_size_unscaled} is rescaled to \code{cohort_stock_size}
within each \code{herd_id}
\item \code{offtake_heads} and \code{offtake_heads_assessment} are rescaled
proportionally from the unscaled cohort stock to the scaled cohort stock
}

When only one module is run, no cross-module rescaling is needed and
\code{cohort_stock_size_unscaled} is simply renamed to \code{cohort_stock_size}.
}
}
\examples{
\dontrun{ 
# Use case 1 - *Run the demographic only*:

cohort_path <- system.file(
  "extdata/run_modules_examples/herd_simulation_input_chrt_data.csv",
  package = "gleam"
)
herd_level_path <- system.file(
  "extdata/run_modules_examples/herd_simulation_input_hrd_data.csv",
  package = "gleam"
)
cohort_data_demographic <- data.table::fread(cohort_path)
herd_level_data <- data.table::fread(herd_level_path)

out <- run_all_herd_module(
  cohort_level_data = cohort_data_demographic,
  herd_level_data = herd_level_data,
  run_demographic = TRUE,
  run_nondemographic = FALSE
)


# Access the results
demo_cohort_results <- out$cohort_level_results
demo_cohort_results
demo_herd_results   <- out$herd_level_results
demo_herd_results

=====

# Use case 2 - *Run the non-demographic only*

cohort_nondemo_path <- system.file(
  "extdata/run_modules_examples/nondemographic_herd_input_chrt_data.csv",
  package = "gleam"
)
herd_nondemo_path <- system.file(
  "extdata/run_modules_examples/nondemographic_herd_input_hrd_data.csv",
  package = "gleam"
)
cohort_nondemo_input <- data.table::fread(cohort_nondemo_path)
herd_level_data_nondemo <- data.table::fread(herd_nondemo_path)

results <- run_all_herd_module(
  cohort_level_data = cohort_nondemo_input,
  herd_level_data = herd_level_data_nondemo,
  run_demographic = FALSE,
  run_nondemographic = TRUE
)


# Access the results
results$cohort_level_results
=====

# Use case 3 - *Run both the demographic and non-demographic*:

cohort_combined_path <- system.file(
  "extdata/run_modules_examples/run_all_herd_module_input_chrt_data.csv",
  package = "gleam"
)
herd_level_path <- system.file(
  "extdata/run_modules_examples/example_herd_level_data.csv",
  package = "gleam"
)
cohort_level_data <- data.table::fread(cohort_combined_path)
herd_level_data <- data.table::fread(herd_level_path)

out <- run_all_herd_module(
  cohort_level_data = cohort_level_data,
  herd_level_data = herd_level_data,
  run_demographic = TRUE,
  run_nondemographic = TRUE
)

# Access the results
combined_cohort <- out$cohort_level_results
combined_cohort
demo_herd <- out$herd_level_results
demo_herd
}

}
\seealso{
\code{\link[=run_gleam]{run_gleam()}},
\code{\link[=run_demographic_herd_module]{run_demographic_herd_module()}},
\code{\link[=run_nondemographic_herd_module]{run_nondemographic_herd_module()}},
\code{\link[=rescale_x_to_y]{rescale_x_to_y()}}
}
