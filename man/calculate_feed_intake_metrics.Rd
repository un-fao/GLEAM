% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_feed_rations.R
\name{calculate_feed_intake_metrics}
\alias{calculate_feed_intake_metrics}
\title{Calculate Feed Intake Metrics}
\usage{
calculate_feed_intake_metrics(rations_share, feed_params, input_feed)
}
\arguments{
\item{rations_share}{A data.table containing feed shares per cohort. Must include:
\itemize{
\item \code{Animal}, \code{GLEAM3_name}, \code{COUNTRY}, \code{ADM0_CODE}, \code{HerdType}, \code{LPS}, \code{cohort}, and \code{value}.
}}

\item{feed_params}{A data.table of nutrient parameters. Must include:
\itemize{
\item \code{GLEAM3_name}, \code{GE}, \code{DE_ruminants}, \code{DE_pigs}, \code{ME_ruminants}, \code{ME_pigs}, \code{ME_chickens}, \code{N_content}.
}}

\item{input_feed}{A data.table of cohort-level baseline GLEAM data. Must include:
\itemize{
\item \code{Animal_short}, \code{ADM0_CODE}, \code{COUNTRY}, \code{HerdType}, \code{LPS}, \code{cohort}.
}}
}
\value{
A data.table matching \code{input_feed} enriched with dietary metrics:
\itemize{
\item \code{diet_ge}, \code{diet_me}, \code{diet_nitrogen}, \code{diet_dig}
}
}
\description{
Computes cohort-level dietary energy, digestibility, and nitrogen intake
from feed rations and nutritional parameters. Assumes inputs are pre-cleaned.
}
\examples{
\dontrun{
# Load cleaned example input from the package and compute feed intake metrics
feed_params <- data.table::fread(
  system.file("extdata/Feed_parameters.csv", package = "gleam")
)

rations_share <- data.table::fread(
  system.file("extdata/GLEAM_input_FeedRations.csv", package = "gleam")
)

input_feed <- data.table::fread(
  system.file("extdata/GLEAM_input_feed.csv", package = "gleam")
)

result <- calculate_feed_intake_metrics(rations_share, feed_params, input_feed)
}
}
